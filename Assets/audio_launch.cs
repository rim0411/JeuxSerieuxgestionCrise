using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class audio_launch : MonoBehaviour
{
    public GameObject alarm;

    // Update is called once per frame
    private void shir()
    {
         alarm.SetActive(true);
    }
}
